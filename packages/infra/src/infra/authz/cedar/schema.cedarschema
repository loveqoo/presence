entity LocalUser { id: String, role: String };
entity User { id: String };
action create_agent appliesTo {
  principal: [LocalUser],
  resource: [User],
  context: {
    currentCount: Long,
    maxAgents:    Long,
  }
};
